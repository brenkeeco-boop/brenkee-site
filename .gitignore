# ===== Geral / sistema =====
.DS_Store
Thumbs.db
*.log

# ===== /admin (React + Vite) =====
admin/node_modules
admin/dist
admin/dist-ssr
admin/*.local

# Variáveis de ambiente do painel (contém a anon/public key do Supabase).
# Nunca deve ir para o repositório — só existe localmente.
admin/.env
admin/.env.local
!admin/.env.example

# Editores
.vscode/*
!.vscode/extensions.json
.idea
*.sw?
